# Configuration settings for build-nim tool

const
  # Command name for CLI interface
  commandName* = "build-nim"

  # Directory name within project root containing tools
  toolsDirectory* = "tools"

  version* = "0.0.0"
