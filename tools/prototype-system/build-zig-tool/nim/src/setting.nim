# Configuration settings for build-zig tool

const
    # Command name for CLI interface
    commandName* = "build-zig"

    # Directory name within project root containing interfaces
    interfacesDirectory* = "interfaces"

    version* = "0.0.0"
