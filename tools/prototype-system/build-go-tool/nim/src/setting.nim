# Configuration settings for build-go tool

const
    # Command name for CLI interface
    commandName* = "build-go"

    # Directory name within project root containing services
    servicesDirectory* = "services"

    version* = "0.0.0"
